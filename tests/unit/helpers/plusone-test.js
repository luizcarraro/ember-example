
import { plusone } from 'ember-example/helpers/plusone';
import { module, test } from 'qunit';

module('Unit | Helper | plusone');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = plusone([42]);
  assert.ok(result);
});

